<x-app-layout>
    <div class="w-full lg:max-w-1/2 py-12">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
            <div class="bg-white dark:bg-gray-800 overflow-hidden shadow-sm sm:rounded-lg">
                <div class="justify-center p-3 text-gray-900 dark:text-gray-100">
                    <h1 class="text-2xl mb-2">{{ __("WIP Policy") }}</h1>
                    <p>{{ __("This is just a simple gimmick app, maintained by a single developer.") }}</p>
                    <p>{{ __("The only info saved are name and email. Which are not used outside this website.") }}</p>
                    <p>{{ __("If for some miracle, enough people actually use this and I ever need an actual TOS and privacy policy, this is where they'll be found.") }}</p>
                </div>
            </div>
        </div>
    </div>
</x-app-layout>